@inject AppState AppState

<input type="text" class="form-control" @bind-value="@repoName" placeholder="Add Repo"/>
<button type="submit" class="btn btn-primary ml-sm-1" @onclick="() => AppState.GetRepoFiles(repoName)" href="">Root Dir</button>

@code{
	string repoName;
}